{
  "name": "schema",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",

  "scripts": {
    "dev":"export PGPASSWORD=6220617 && npm run db:drop && npm run db:create && npm run db:seed && npm run db:serve",
    "db:dev":"export PGPASSWORD=6220617 && npm run db:drop && npm run db:create && npm run db:seed",
    "db:drop":"psql -U jalal -d cashplay_dev -a -f ./db/utils/drop-schema.sql",
    "db:seed":"psql -U jalal -d cashplay_dev -a -f ./db/utils/seed.sql",
    "db:create":"psql -U jalal -d cashplay_dev -a -f ./db/utils/create-schema.sql",
    "db:regen":"psql -U jalal -d cashplay_dev -a -f ./db/utils/regen.sql",
    "db:serve":"postgraphql -c posgres://cashplay_postgraphql:admin@localhost:5432/cashplay_dev -s cashplay -r cashplay_anonymous -o http://localhost:3000 --classic-ids -e foobar -t cashplay.jwt_token -w -i /graphiql"
  }
}
